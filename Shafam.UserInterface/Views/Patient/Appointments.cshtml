@using Shafam.Common.DataModel
@model Shafam.UserInterface.Models.PatientScheduleViewModel

@{
    ViewBag.Title = "Appointments";
}

@Html.Partial("_Header")

<div class="row">
    <div class="col-md-10">
        <h3>Schedule</h3>
    </div>
    <div class="col-md-2">
        <br />

        <p class="pull-right">@Html.ActionLink("Request Appointment", "RequestAppointment", new { patientId = @Model.Patient.PatientId })</p>

    </div>
</div>

<table class="table">
    <tr>
        <th>Date & Time</th>
        <th>Reason</th>
    </tr>
    @foreach (Appointment appointment in Model.Appointments)
    {
        <tr>
            <td>@appointment.DateTime</td>
            <td>@appointment.Reason</td>
        </tr>
    }
</table>